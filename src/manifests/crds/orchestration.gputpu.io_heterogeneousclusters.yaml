---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: heterogeneousclusters.orchestration.gputpu.io
spec:
  group: orchestration.gputpu.io
  names:
    kind: HeterogeneousCluster
    listKind: HeterogeneousClusterList
    plural: heterogeneousclusters
    singular: heterogeneouscluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HeterogeneousClusterSpec is the desired configuration for
              one Slurm cluster.
            properties:
              authentication:
                properties:
                  jwtKeySecretRef:
                    maxLength: 253
                    minLength: 1
                    type: string
                  mungeKeySecretRef:
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - jwtKeySecretRef
                - mungeKeySecretRef
                type: object
              checkpoint:
                properties:
                  bucket:
                    maxLength: 63
                    minLength: 1
                    type: string
                  credentialsSecretRef:
                    maxLength: 253
                    minLength: 1
                    type: string
                  endpoint:
                    maxLength: 2048
                    minLength: 1
                    type: string
                  failureGracePeriod:
                    default: 2m
                    type: string
                    x-kubernetes-validations:
                    - message: failureGracePeriod must be positive
                      rule: duration(self) > duration('0s')
                  interval:
                    default: 5m
                    type: string
                    x-kubernetes-validations:
                    - message: interval must be positive
                      rule: duration(self) > duration('0s')
                required:
                - bucket
                - credentialsSecretRef
                - endpoint
                - failureGracePeriod
                - interval
                type: object
                x-kubernetes-validations:
                - message: checkpoint endpoint must use HTTPS
                  rule: url(self.endpoint).getScheme() == 'https'
              controlPlane:
                properties:
                  accounting:
                    properties:
                      databaseSecretRef:
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - databaseSecretRef
                    type: object
                  controllers:
                    properties:
                      image:
                        description: Image is used by every Slurm control-plane component.
                        maxLength: 512
                        minLength: 1
                        type: string
                      replicas:
                        default: 2
                        enum:
                        - 2
                        format: int32
                        type: integer
                      stateSaveClaim:
                        maxLength: 253
                        minLength: 1
                        type: string
                        x-kubernetes-validations:
                        - message: stateSaveClaim is immutable
                          rule: self == oldSelf
                    required:
                    - image
                    - replicas
                    - stateSaveClaim
                    type: object
                  login:
                    properties:
                      replicas:
                        default: 1
                        format: int32
                        maximum: 32
                        minimum: 1
                        type: integer
                    required:
                    - replicas
                    type: object
                  rest:
                    properties:
                      replicas:
                        default: 2
                        enum:
                        - 2
                        format: int32
                        type: integer
                    required:
                    - replicas
                    type: object
                required:
                - accounting
                - controllers
                - login
                - rest
                type: object
              recovery:
                properties:
                  automaticReboot:
                    default: false
                    type: boolean
                  maxAutomaticReboots:
                    default: 1
                    format: int32
                    maximum: 1
                    minimum: 0
                    type: integer
                  verificationTimeout:
                    default: 2m
                    type: string
                    x-kubernetes-validations:
                    - message: verificationTimeout must be positive
                      rule: duration(self) > duration('0s')
                required:
                - automaticReboot
                - maxAutomaticReboots
                - verificationTimeout
                type: object
              slurmVersion:
                default: 25.11.7
                enum:
                - 25.11.7
                type: string
              workerPools:
                items:
                  properties:
                    memoryUnit:
                      default: 1Gi
                      description: Fixed memory devices are expressed as positive
                        whole MiB or GiB values.
                      maxLength: 12
                      pattern: ^[1-9][0-9]*(Mi|Gi)$
                      type: string
                    name:
                      maxLength: 63
                      pattern: ^[a-z][a-z0-9-]{0,62}$
                      type: string
                    nodeSelector:
                      additionalProperties:
                        type: string
                      type: object
                    partition:
                      maxLength: 64
                      pattern: ^[a-z][a-z0-9_]{0,63}$
                      type: string
                    profiles:
                      items:
                        properties:
                          deviceClassName:
                            maxLength: 253
                            pattern: ^[a-z0-9]([a-z0-9.-]*[a-z0-9])?/[a-z0-9]([a-z0-9._-]*[a-z0-9])?$
                            type: string
                          gres:
                            maxLength: 64
                            pattern: ^(gpu|tpu):[a-z0-9_]+$
                            type: string
                          name:
                            maxLength: 63
                            pattern: ^[a-z][a-z0-9-]{0,62}$
                            type: string
                        required:
                        - deviceClassName
                        - gres
                        - name
                        type: object
                      maxItems: 8
                      type: array
                      x-kubernetes-list-map-keys:
                      - gres
                      x-kubernetes-list-type: map
                    scaling:
                      properties:
                        idleTimeout:
                          default: 5m
                          type: string
                          x-kubernetes-validations:
                          - message: idleTimeout must be positive
                            rule: duration(self) > duration('0s')
                        maxWorkers:
                          description: Eight pools at this ceiling stay within Slurm's
                            65,536-node limit.
                          format: int32
                          maximum: 8192
                          minimum: 1
                          type: integer
                        minReady:
                          default: 0
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - idleTimeout
                      - maxWorkers
                      - minReady
                      type: object
                      x-kubernetes-validations:
                      - message: minReady cannot exceed maxWorkers
                        rule: self.minReady <= self.maxWorkers
                  required:
                  - memoryUnit
                  - name
                  - partition
                  - scaling
                  type: object
                maxItems: 8
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - authentication
            - controlPlane
            - slurmVersion
            - workerPools
            type: object
            x-kubernetes-validations:
            - message: worker pool partitions must be unique
              rule: self.workerPools.all(x, self.workerPools.filter(y, y.partition
                == x.partition).size() == 1)
            - message: automatic recovery requires at least one worker profile
              rule: '!has(self.recovery) || !self.recovery.automaticReboot || self.workerPools.exists(p,
                p.profiles.size() > 0)'
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              workerPools:
                items:
                  properties:
                    draining:
                      format: int32
                      type: integer
                    name:
                      type: string
                    pending:
                      format: int32
                      type: integer
                    ready:
                      format: int32
                      type: integer
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
